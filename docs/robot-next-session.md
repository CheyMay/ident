# Следующий Контрольный Сеанс

## Граница Готовности

Выпуск агента 2.14.4 установлен на клинике: диагностика 09.09.2026 в 15:15:10 UTC
подтвердила succeeded, работающий worker и выгрузку расписания. Робот выключен.
Статусы показа проверены на реальном сеансе: captured, 4 снимка, затем completed
и archiveReady. Новое обновление нельзя назначать во время активного скана или
показа. Очередь не очищается, приемы не создаются.

Полученный архив проверен локально: [разбор четырех состояний](robot-training-audit-2026-09-09.md).
Найдена и локально исправлена пропущенная привязка поля фамилии `_surnameTextBox`.
Привязка включена в установленный пакет 2.14.5. Это не готовый адаптер
календаря/поиска пациента.

### Пассивный Показ В 2.14.5

Второй реальный сеанс завершился по лимиту в 19:40:20 +03:00: один снимок,
1030 элементов; попытка снять меню была отклонена с wrong_window, а не зависла.
Оператор подтвердил экспорт; сервер затем подтвердил completed, archiveReady,
captured=1. Сохраненные файлы не пропали.

Подготовлены: отдельный постоянный счетчик и таймер 15 минут, отменяемый
снимок через 8 секунд, звуковой сигнал при отказе проверки окна, видимый
Проводник после экспорта. Ошибка открытия Проводника больше не помечает
успешный архив как поврежденный. После экспорта очищается старый код ошибки.

Пассивная проверка использует точный HWND и PID; заголовок самого окна либо
его root owner допустим только в том же процессе. Это устраняет зависимость
от Process.MainWindowTitle для отдельных форм. Сканер повторяет проверку;
он не заменяет закрывшееся окно календарем и не снимает весь рабочий стол.
Основание: [GetAncestor](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-getancestor),
[GetWindowTextW](https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-getwindowtextw).

Задержка не снимает ограничение на IDENT и не нажимает кнопки в нем. Нужен
контрольный скан меню на клинике: unit-тесты не доказывают, что всплывающее
меню доступно через UI Automation. «Разделить интервал» пока не автоматизировано:
оператор уточнил, что для 45 минут разделение может требоваться до выделения.
До выяснения его побочного эффекта не выбирать этот пункт ради обучения.

Выпуск 2.14.5 опубликован 09.09.2026 в 16:53:26 UTC и назначен только после
completed/неактивного показа, свежего heartbeat и подтверждения выключенного
робота. SHA256 ZIP (84 760 байт):
`FFF856C4A31DBCF78B7218B7194D3297087764AD50B04F3BE79CE46CA5555CBC`.
Диагностика, полученная в 16:54:42 UTC: версия 2.14.5, update=succeeded,
schedule=ok, последняя успешная выгрузка 19:54:37 +03:00, robotEnabled=false.
Старое окно показа нужно закрыть и начать новый показ; установщик не требуется.

Пройдены 9 точечных проверок: CaptureTarget, Observation, Training,
CaptureTelemetry, CapturePrivacy, CalibrationFlow, RobotSafety, AgentUpdate,
InstallerPackage (явно указан архив 2.14.5). Отдельно проверены синтаксис,
кодировка и рендер формы. Первый запуск InstallerPackage без параметра проверил
его старый архив по умолчанию 2.4.1, поэтому был повторен на новом пакете.

История предыдущей проверки: пакет 2.14.2 опубликован 09.09.2026 в 08:18:59 UTC,
на момент этой проверки не был назначен. Его SHA256:
`b05e5d6b37131ed24ab65444eaf006967c2ade2867e340139ea0abbd59fe1b26`.
Проверка сервера 09.09.2026 в 08:18:46 UTC: был установлен
и по-прежнему назначен 2.12.1, робот выключен и не настроен. Обновление до
нового выпуска требует отдельного назначения при контролируемом запуске.

## Статус Показа В 2.14.4

### Уточнение Рабочего Сценария

09.09.2026 оператор подтвердил: врач и время задаются выбором интервала в
календаре IDENT; в открытой форме нового приема поменять их нельзя. Это
сведения оператора, а не подтверждение работы автоматического выбора.
Не требовать сканов несуществующих настроек врача/времени в форме приема.

Для адаптера нужен отдельный проверенный переход: дата и врач в календаре,
свободный интервал, открытие нового приема, сверка врача и времени открытого
окна с заявкой, затем поиск/выбор пациента. Ввод DoctorName/PlanStart/PlanEnd
в поля формы не заменяет этот переход. Оператор уточнил выбор длительности:
зажать ЛКМ и протянуть интервал в колонке врача, затем ПКМ по выделенной
области и первый пункт «Записать на прием...». На предоставленном скриншоте
видны выделенная область и это контекстное меню. Второй пункт продолжения
записи из буфера не относится к созданию новой заявки и использоваться не должен.
Нужны UI-сканы выделения, меню и открытого из него приема с проверкой границ
времени. Скриншот не определяет надежную привязку координат или доступность
UI Automation для протягивания: автоматическое выделение еще не проверено.
При несовпадении врача или интервала заполнять и сохранять прием нельзя.
Два сохраненных скана (календарь и форма приема) не подтверждают готовность
этих действий к автоматическому исполнению.

### Передача Состояния

Панель и показ сохраняют небольшой локальный статус. Worker добавляет в
обычный heartbeat плоские поля diagnostics.training* и diagnostics.calibration*.
Существующий backend принимает их без изменения API. Во время активного показа
интервал отправки сокращается примерно до 5 секунд, в том числе при ожидании
дочернего процесса. Это не гарантия задержки: сеть и блокирующие операции могут
замедлить доставку. После завершения действует обычный интервал.

Передаются только ID сеанса, время, состояние, числовые счетчики, признак
готовности архива и разрешенный код ошибки. Произвольный текст ошибок, UI-tree,
скриншоты, конфиги и данные пациентов не передаются. Архив передается отдельно
и явно. Ошибочный/слишком большой файл статуса не прерывает heartbeat.

Состояния: not_started, waiting, scanning, captured, failed, completed, closed,
expired. Если активное окно не обновляло статус более 20 секунд, отображается
unresponsive; неверный файл дает unavailable. Старое время heartbeat также
нужно учитывать: сохраненный сервером статус не доказывает текущую связь.
trainingHotkeys показывает число принятых нажатий вне активного скана,
trainingAttempt число попыток, trainingCaptured число сохраненных состояний.
Калибровка completed означает только свежий скан, не активацию профиля.

Проверка Test-IdentCaptureTelemetry покрывает переходы, приватность, устаревание,
ограничение частоты, сбой сети, поврежденные файлы и отсутствие включения робота.
После обновления нужно заново открыть панель/показ: уже открытое окно исполняет
старый код, даже если версия в заголовке состояния изменилась.

Пакет 2.14.4 опубликован 09.09.2026 в 15:06:21 UTC; назначен в 15:13:59 UTC.
SHA256: `ce93ab4292b0cb20f45019c55a199a86d646840cc27a5feba26a1659c2d14c8a`.
Пройдены 12 локальных проверок: CaptureTelemetry, Training, CalibrationFlow,
Reliability, Observation, CapturePrivacy, ChildLifetime, WorkerLifecycle,
AgentUpdate, InstallerPackage, UiScan, RobotSafety. Проверены синтаксис/BOM
9 PowerShell-файлов и рендер окна показа. После обновления передача live-статуса
подтверждена на ПК клиники; готовность к реальной записи этим не подтверждается.

**Автозапись пока не готова к приемке.** Исправление сканера и успешные тесты
защит не заменяют проверку действий в настоящем IDENT. Работающий шаблон ввода
единого ФИО нельзя использовать для раздельных полей фамилии, имени и отчества.
Поддержка явных ClientSurname, ClientName и ClientPatronymic подготовлена в
2.14.0; порядок слов единого ФИО не угадывается. Пустое отчество задается явно.
Старые заявки с одним ClientFullName не могут автоматически исполняться
раздельным профилем. Ввод частей ФИО из amoCRM требует отдельной настройки
контракта формы; это изменение не было развернуто на backend или в виджете.
Нужны фактические элементы формы, выбор/создание пациента и адаптация переходов
врача/даты/времени. Эти шаги не подменяются предположениями из скриншотов.

## Подготовлено До Сеанса В 2.14.0

- «Начать показ экранов»: Ctrl+Alt+F8 в IDENT сохраняет очередное состояние
  без переключения в PowerShell. Никаких нажатий, ввода, сохранения, активации
  профиля или отправки файлов в сеть этот режим не выполняет.
- Один сканер одновременно, 60 секунд на попытку, до 12 попыток за 15 минут.
  Завершенные состояния имеют отдельные ID/время/хеш и не перезаписываются.
- Exclusive file lease не допускает запуск показа во время исполнения и
  получение новой заявки во время показа. Worker и его child используют
  совместимые read leases. Закрытие процесса освобождает lease без ручного
  удаления файла. Старый worker до 2.14.0 этот протокол не поддерживает.
- Сканер привязан к Windows Job с KillOnJobClose, включая разовую проверку.
  Аварийное закрытие владельца завершает его сканер. Основа механизма:
  https://learn.microsoft.com/en-us/windows/win32/procthread/job-objects
- IDENT-training.zip содержит только ui-tree, технические
  сводки (без локальных конфигов) и индекс состояний. Сами ui-tree могут
  содержать персональные данные, поэтому передаются только явно и приватно.
- Кандидат calibration-split-candidate.json готовится локально отдельно от
  действующего профиля и остается запрещенным для исполнения. Его наличие
  не доказывает корректность поиска пациента или выбора времени.

Цель режима показа: за один короткий сеанс получить последовательность
реальных состояний без ручных команд и путаницы с файлами. Это не обучение
по видеозаписи и не кнопка автоматического допуска к записи.

## Что Проверено Локально

- Windows PowerShell 5.1: экспорт пустого, одного и нескольких корней UI.
- Приоритет видимого отдельного окна приема перед большим календарем.
- Отказ от старого идентификатора запуска, неверного времени/хеша/формата файла,
  подмены пути и свёрнутого окна. Ошибка возвращает доступность кнопки проверки.
- Скан не меняет действующий профиль и не разрешает выполнение. Нераспознанные
  поля не наследуют старые привязки в кандидате профиля.
- Фамилия не подбирается как единое ФИО; отдельные роли видны в отчете.
- До получения заявки отклоняются профили с одинаковым полем для разных данных,
  неподдерживаемыми действиями, безымянными элементами и кнопкой сохранения
  вместо отдельного признака успеха. Прошедшая дата приема блокируется до ввода.
- При активности за последние 60 секунд и блокировке сеанса заявка не берется.
- Потеря ответа сервера: одно выполнение, две доставки подтверждения, без дубля.
- Неопределенный результат оставляет очередь на проверке; пересекающиеся слоты
  и устаревшее расписание блокируются в локальных серверных тестах.
- Зависший тестовый worker перезапускается; закрытый файл состояния не обрушает
  supervisor. Обновление сохраняет секреты/маппинг/профиль; повреждение дает откат.

Проверки: Test-IdentUiScan, Test-IdentCalibrationFlow, Test-IdentRobotSafety,
Test-IdentReliability, Test-IdentWorkerLifecycle, Test-IdentSupervisor,
Test-IdentAgentUpdate; 18 серверных тестов данных пациента, резервирования и
хранилища. Панель проверена на тестовом состоянии, не на клиентском IDENT.
Серверные тесты подтверждают локальный код, не версию, развернутую в production.

В 2.14.0 повторно пройдены перечисленные Windows-проверки, включая упаковку
единственного установщика и визуальный рендер обеих панелей. Новый
Test-IdentTraining проверяет несовместимость показа с исполнением, отсутствие
claim, раздельные поля, отсутствие угадывания ФИО, накопление двух состояний,
состав ZIP, неверный ID, ошибку сканера, таймаут и гибель родительского процесса.
Тест worker: одно выполнение, один claim, две доставки complete, ноль fail.
Серверный код/виджет в этом выпуске не менялись; серверные тесты выше относятся
к предыдущему аудиту, а не к развертыванию нового backend.

## Повторный Аудит 2.14.1

Повторный аудит устраняет три воспроизведенных/проверенных риска:

- Worker привязывает дочерние процессы к Windows Job. Тест аварийного закрытия
  владельца воспроизводил продолжающий работать дочерний процесс до исправления;
  теперь дочерний процесс завершается. Таймаут и отказ телеметрии также завершают
  дочерние процессы. Неопределенный результат записи остается на ручной проверке.
- Inspect, Calibrate и Verify не создают скриншоты даже при ошибке. Исполнение
  больше не сохраняет весь рабочий стол, если окно IDENT недоступно или неактивно.
- Поврежденный профиль проверяется до запуска показа; аварийное завершение показа
  отображается в панели, технический журнал остается локально. Активный профиль
  не изменяется ни при ошибке, ни при успешном сборе состояний.

Это не подтверждение реального ввода в IDENT и не замена контрольного приема.

Повторно пройдены 11 Windows-проверок: Test-IdentChildLifetime,
Test-IdentCapturePrivacy, Test-IdentTraining, Test-IdentWorkerLifecycle,
Test-IdentReliability, Test-IdentCalibrationFlow, Test-IdentUiScan,
Test-IdentRobotSafety, Test-IdentSupervisor, Test-IdentAgentUpdate,
Test-IdentInstallerPackage. Пакет обновления проверен с откатом, клиентский
архив содержит один установщик и инструкцию. Синтаксис/BOM 13 PowerShell-файлов
проверены; обе панели визуально проверены на тестовом состоянии.

Повторно пройдены 18 локальных серверных тестов booking-patient,
slot-reservations и storage: дата рождения, максимум 6 часов, резервирование
всех частей интервала, конкурирующие заявки, устаревшая выгрузка, неопределенный
результат сохранения и восстановление квитанции без повторной записи.
Backend и виджет не изменялись и не развертывались в этом выпуске.

Сервер отвечает, флаги настройки OAuth и наличия токена amoCRM положительные;
это не новая проверка запросов к самому amoCRM. Выгрузка клиники на 07:35:36 UTC
успешна: 8 врачей, 3047 интервалов, 488 услуг. В очереди две старые заявки,
ни одна не исполнялась в рамках аудита. Реальная перезагрузка Windows и
выполнение записи в клиентском IDENT в этом сеансе не проверялись.

Клиентский архив IDENT-Client-2.14.1.zip имеет SHA256:
`6cfcd5f490fc3835b3bd872df3fd8789931427c804ed95dfcbe1f22f2ff427c3`.

## Проверка Перед Запуском 2.14.2

- Воспроизведено аварийное закрытие supervisor: до исправления worker оставался
  работать без надзора. Теперь отдельный Windows Job завершает worker; тот
  закрывает свои jobs робота/выгрузки. Updater намеренно не привязан к этому job.
  Проверено и прямое завершение процесса, и остановка реальной временной задачи
  Планировщика Windows. Тестовая задача удалена после проверки.
- Обновляющий компонент перезапускает работающую задачу надзора, чтобы загрузить
  новый код. До перезапуска сохраняется статус applying, чтобы старая версия
  надзора не успела начать запись между обновлением файлов и своим завершением.
- В supervisor-state.json фиксируется SHA256 загруженного скрипта, а в удаленной
  диагностике autostart появляются supervisorCodeSha256 и supervisorStartedAt.
  Это идентификатор реально запущенного кода, не версия из config.local.json.
- Повторная первичная установка отклоняется до любых изменений, если существуют
  config.local.json или secrets.local.json. Тест до исправления воспроизвел
  перезапись действующего worker. Для обновления используется отдельный механизм
  с сохранением конфигурации и откатом, а не повторное заполнение установщика.
- Скрыт PowerShell-хост панели состояния при автозапуске через задачу Windows.

Переход с установленного 2.12.1: первый пакет исполняет СТАРЫЙ updater. Поэтому
нельзя считать, что наличие версии 2.14.2 уже подтверждает обновление запущенного
supervisor. Нужен контролируемый перезапуск задачи надзора и проверка его хеша.
Кнопка перезапуска worker сама по себе не обновляет код supervisor. Не выполнять
перезапуск во время записи, показа экранов или применения обновления.

59 локальных серверных тестов пройдены полностью с одной последовательной
очередью выполнения. Реальное чтение схемы amoCRM успешно 09.09.2026 08:10:48 UTC:
23 поля сделки, 4 воронки. Поле «Время записи» (2045661, date_time) существует,
но planStart не привязан. «Доктор» (2045663, text) также найден. Настройки не
менялись: включение импорта существующих сделок требует отдельной проверки,
чтобы старые сделки не стали новыми заявками. Виджет передает время напрямую.

Браузерный тест widget-browser-check.mjs проверил стенд виджета 1.24.0:
открытие одним кликом, отсутствие пересечения кнопки с задачей, несколько врачей,
31 день, исключенного врача, диапазон длительностей 15..360 минут, будущую дату
рождения, запрос на 6 часов и резерв всех 24 частей, блокировку повторной отправки,
стартовую вкладку календаря и рамки окна на desktop/mobile. Скриншоты просмотрены.
Стенд использует синтетические данные и запрещает реальные запросы к backend;
это не проверка установленного виджета в живом DOM amoCRM. Рабочий widget.zip
прошел структурную проверку. Frontend/backend в выпуске 2.14.2 не изменялись.

Механика Windows сверена с официальным описанием Job Objects и флагов:
https://learn.microsoft.com/en-us/windows/win32/api/winnt/ns-winnt-jobobject_basic_limit_information

Для 2.14.2 пройдены все 11 Windows-проверок предыдущего аудита, плюс новые
Test-IdentSupervisorLifetime (обычный процесс и настоящая задача Windows),
Test-IdentTaskRestart и Test-IdentSetupPreservation: всего 14 сценариев.
Синтаксис/BOM всех 14 измененных или новых PowerShell-файлов проверены.
Обе панели агента отрендерены и просмотрены. Временных задач Code9 IDENT QA
после проверок не осталось. Настоящую перезагрузку ПК клиники не выполняли.

Хеш загруженного supervisor для этого архива должен совпасть с:
`b866f904c07b38adc1f91ed617ff7a007a024c9011043f9418570eb4dce2d54e`.
SHA256 клиентского архива для ПЕРВОЙ установки IDENT-Client-2.14.2.zip:
`d8fe775bf22b915b9c0028af094485d250eec99a7f72fe98f1b78a9901dd4697`.
Рабочий widget.zip побайтно совпадает с проверенными manifest/script/style/i18n
версии 1.24.0; повторно переустанавливать виджет ради этого аудита не нужно.

Фактическая серверная выгрузка на 08:16:38 UTC содержит 3047 интервалов на
31 день: с 09.09.2026 09:00 до 09.10.2026 18:45 по переданному расписанию.
В справочнике 8 врачей, но интервалы есть только у 4. У остальных нужно сверить
назначенные смены и SQL-маппинг на клиентском IDENT. Пустые интервалы не означают
свободное время, и создавать их предположительно нельзя.

## Порядок Сеанса

### Уточнение Показа В 2.14.3

Пакет опубликован 09.09.2026 в 11:09:20 UTC, но клиенту не назначен.
SHA256: `cf3f6b9813bdefea75cf20d9910531768bd9cd9c497260f465f99eba718ffb73`.
После публикации клиент на связи: сигнал 11:09:04 UTC, установлен и назначен
2.12.1, робот выключен и не настроен. Live-конфигурация и заявки не изменялись.

- Показ использует отдельный режим Observe, а не автоматическую калибровку.
  При Ctrl+Alt+F8 запоминаются HWND и процесс активного окна IDENT. Сканируется
  именно это окно, без приоритета большого окна «Новый прием» над настройками.
- Маленькому диалогу достаточно одного видимого элемента для сохранения текста.
  Это критерий наблюдения, не готовности селекторов или автоматической записи.
- Закрытие, скрытие, сворачивание или смена идентичности окна отклоняют попытку.
  Проверки выполняются до и после обхода; вместо закрытого диалога календарь
  не подставляется. Используются UIA и проверки Windows IsWindowVisible/IsIconic:
  https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-iswindowvisible
  https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-isiconic
- Observe не создает и не активирует профили. Архив содержит исходный текст
  выбранных окон для последующей настройки специалистом. Ошибка скана и успех
  обозначаются разными звуковыми сигналами. Нельзя продолжить закрытый сеанс
  после экспорта или истечения 15 минут; повторный показ запускается отдельно.
- Удален оставшийся вызов снимка экрана при неполной разовой калибровке.
  Проверены также ошибки Observe: конфигурация не меняется, снимки не создаются.
- Новая Test-IdentObservation воспроизводит скрытый HWND, маленькую форму,
  закрытие, замену окна и чужой процесс. У Test-IdentTraining добавлены проверки
  возврата другого HWND, отсутствия цели, лимита времени и закрытого сеанса.

Повторно пройдены восемь последовательных локальных проверок Windows:
Observation, Training, CalibrationFlow, UiScan, CapturePrivacy, RobotSafety,
AgentUpdate и InstallerPackage. Проверены синтаксис/BOM десяти PowerShell-файлов,
окно показа отрендерено и просмотрено. Выпуск не меняет backend или виджет.

Это проверка локальных механизмов, а не доказательство доступности всех полей
клиентского IDENT. Невсплывающее отдельным активным окном меню может входить
в дерево родителя; наличие его пунктов нужно проверить в реальном архиве.
Предельные 40 секунд/5000 элементов обхода и внешний таймаут 60 секунд сохраняются.
Ограничение перехода с 2.12.1 через старый updater, описанное выше, остается.

Короткая клиентская инструкция: [Показ экранов](robot-training-quickstart.md).

### Контролируемый Запуск

1. Проверить связь и свежесть расписания; робот выключен. Установить проверенный
   выпуск через админский механизм и дождаться сообщения о его версии.
2. Нажать «Начать показ экранов», подтвердить локальное сохранение текста UI.
   На каждом нужном экране IDENT нажать Ctrl+Alt+F8 и дождаться сигнала.
   Снять календарь, меню свободного времени, форму, выбор пациента и настройки
   приема. После показа собрать архив одной кнопкой и передать специалисту.
   Альтернатива: разовый скан с восьмисекундной задержкой и кнопкой копирования.
3. Специалист проверяет элементы отдельного ФИО, поиск/выбор существующего
   пациента, создание нового, телефон с маской, дату рождения и комментарий.
   Отдельно снять экран/скан настроек врача, даты и начала/конца приема.
4. Подтвердить, что нужный день, врач и интервал могут открываться из календаря.
   Не заменять отсутствующие элементы случайными координатами.
5. Проверить заполнение под наблюдением без сохранения и без уведомлений.
   Нынешний DryRun только ищет элементы, он не выполняет пробный ввод.
6. Согласовать одного тестового пациента, слот и длительность. Разобрать две
   старые заявки; не давать роботу автоматически исполнять их ради теста.
7. Выполнить одну контролируемую запись. В самом IDENT проверить пациента,
   дату рождения, врача, филиал, дату, начало/конец и комментарий. Убедиться,
   что создан ровно один прием. Проверить обновление расписания и блокировку
   всего интервала в amoCRM, включая 45 минут и 2 часа в отдельных проверках.
8. Только после успешного полного пути решать вопрос включения очереди.

## Если Что-то Не Совпало

- Форма закрылась: остановиться, получить новое состояние; не продолжать ввод
  по старым координатам/адресам элементов.
- Ошибка скана: снимок панели и новый журнал; не пересылать старый ui-tree.json.
- Ошибка после возможного сохранения: сначала проверить IDENT, не повторять
  заявку и не удалять execution-pending.json или квитанцию подтверждения.
- Срочная реальная запись: штатный ручной ввод в IDENT с отдельной сверкой
  заявки amoCRM. Наличие заявки в очереди не означает подтвержденный прием.

Никакие проверки не отправляют снимки или данные полей пациентов автоматически.
Удаленная диагностика содержит только идентификатор/время скана, счетчики,
контрольную сумму и флаги результата. Полный UI-скан передается специалисту
явно, не кладется в Git, установщик или общедоступные материалы.
